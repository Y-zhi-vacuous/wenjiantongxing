# MEMORY.md — 文鉴同行 完整开发总结

## 项目概要

「文鉴同行」是一个 AI 驱动的深圳中考作文批改平台，通过大模型实现手写作文 OCR 识别、五维度智能评分、个性化能力画像分析。支持 Web 端和 Android APK，部署在 Sealos 免费 K8s 平台。

- **公网地址**：`https://wppyqjhwlqso.usw-1.sealos.app`
- **GitHub**：`https://github.com/Y-zhi-vacuous/wenjiantongxing`
- **v1.0 开发周期**：2026-04-30 ~ 2026-05-02（约 30 小时）
- **v2.0 开发周期**：2026-05-02（架构重构）

---

## v2.0 架构重构

### 核心变化

| 方面 | v1.0 | v2.0 |
|------|------|------|
| 评分触发 | 学生提交后自动触发 | 教师手动触发 |
| AI 配置 | 学生端统一配置 | OCR(学生) + 评分/能力(教师) 分离 |
| 模型提供商 | 仅智谱 | 6 提供商 (Zhipu/OpenAI/DeepSeek/Claude/Ollama/vLLM) |
| 能力分析 | 每次批改后自动触发 | 教师触发（使用教师配置的模型） |
| Prompt 管理 | 内联在代码中 | 独立 prompts/ 模块 |
| 批量处理 | 无 | 一键全部批改（串行逐篇） |

### 新架构数据流

```
学生端                         教师端
  │                              │
  ├── OCRConfig ──→ OCR识别      ├── GradingConfig ──→ call_llm → 6 providers
  ├── 提交作文                    ├── 单篇批改
  └── 查看报告                    ├── 一键全部批改 (串行逐篇)
                                  ├── 能力分析刷新
                                  └── 教学建议 + 错误模式
```

---

## 一、沟通协作模式

本项目采用 **AI 辅助全栈开发** 的协作模式。用户负责需求定义与决策，AI 负责设计、编码、部署、调试全流程。

### 关键沟通节点

| 阶段 | 沟通方式 | 典型案例 |
|------|----------|----------|
| 需求澄清 | AI 提问 → 用户单选/描述 | v2.0: 评分能力分离到教师端 |
| 架构选型 | AI 提方案+优劣 → 用户选 | 6 提供商统一路由 vs 各模型独立调用 |
| 问题反馈 | 用户描述现象 → AI 看日志定位 | v1.0: OCR 6轮迭代 |
| 功能迭代 | 用户提改进方向 → AI 实现 | v2.0: 一键全部批改 + 教学建议 |

---

## 二、项目架构演进

### v1.0 最终架构
```
学生提交 → [OCR 容错链] → [两步法评分] → [能力分析]
```

### v2.0 最终架构
```
学生端: OCR 容错链 (学生OCRConfig) → 提交作文
教师端: 两步法评分 (教师GradingConfig + call_llm) → 能力分析 (教师触发)
```

### AI 系统 v2.0

**统一路由层 (call_llm)**
- 云端: Zhipu / OpenAI / DeepSeek → OpenAI-compatible API
- 云端: Claude → Anthropic API (消息格式自动转换)
- 本地: Ollama → /api/chat
- 本地: vLLM → /v1/chat/completions

**两步法评分改进**
- 切题检查改为 JSON 输出: `{"topic_match": "切题/偏题/离题", "confidence": 0.95, "reasoning": "..."}`
- 使用教师 GradingConfig 配置的提供商和模型

**能力分析改进**
- 新增 teaching_recommendations (本周/2周/1月)
- 新增 error_patterns (跨作文共性错误识别)
- 使用教师 GradingConfig 的 ability_model_name（可独立于评分模型）

---

## 三、核心问题与解决过程

### v1.0 关键问题（全部解决）
| 问题 | 解决方案 |
|------|----------|
| 前端白屏 | ErrorBoundary 组件 |
| Git push 时断时续 | SSH Key |
| OCR 6 轮不工作 | thinking:disabled + 重试 + 降级 + 压缩 |
| AI 打高分 | 两步法: 独立切题检查 + 注入判定 |
| 能力分析千篇一律 | AI 驱动替代模板匹配 |
| Sealos 容器崩溃 | bcrypt版本/SQLite路径/seed初始化 |

### v2.0 新增架构决策
| 决策 | 理由 |
|------|------|
| 评分迁移教师端 | 教师控制评分质量，可选付费API或本地部署 |
| 统一 call_llm 接口 | 屏蔽 6 个提供商的 API 差异 |
| 串行逐篇批改 | 避免并发 API 限流，单篇失败不影响其他 |
| Prompt 独立模块 | 便于迭代调优，代码与 Prompt 分离 |
| 幂等数据迁移 | 可重复执行，不破坏旧数据 |

---

## 四、项目统计

| 类别 | v1.0 | v2.0 |
|------|------|------|
| 数据模型 | 8 张表 | 10 张表 (新增 OCRConfig, GradingConfig) |
| API 路由组 | 7 组 | 7 组 (重构) |
| 学生端页面 | 8 | 8 (修改) |
| 教师端页面 | 7 | 8 (新增 GradingQueue) |
| LLM 提供商 | 1 | 6 |
| Prompt 模块 | 内联 | 2 个独立模块 |
